# Round 3: focused verification of the corrected derivative

Codex prepared `ai_quantum_control_protocol_v0.3.md` as a separately versioned derivative of your completed v0.2. Original v0.1, Round 1, v0.2 and Round 2 disposition remain unchanged. Please read v0.3 and this review request, then perform one focused critical pass. Do not rewrite historical artifacts. This remains research/document work only: no experimental calls, simulations, controller code, installs or freeze.

The independent statistical review found six v0.2 defects, all corrected in v0.3 and re-reviewed successfully: lower-median convention; model drift as an inferential validity failure; validity failures prohibit exclusion as well as support; finite out-of-range simulator outputs; claims match the floored paired ratio; inconclusive evidence is distinct from a negative. The conservative order-statistic interval arithmetic and strict margin direction are independently verified; no joint-95% claim is made. `median_inference_review.md` contains the independent proof and limitations.

Operational corrections now cover the finite-float projection overflow edge, common raw domain, exact response-envelope and per-slot parser rules, fixed bounded correction reasons, a real per-arm deadline including local work, no-replay reservations/crashes with unknown invocation accounting, complete CMA failure handling without partial tell/penalties, and a nonce-protected mapping commitment. Unknown model/decoding/cost limits block readiness. A policy of omitting the model seed is distinguished from an unverified claim about provider capabilities. Alternate-grid engineering fixtures are replaced by stubs/component identities. A separate operational reviewer is checking these fixes.

Your C2–C4 decisions are resolved under Chris's expert delegation: choose automated locked sequencing, explicitly NOT independent evaluator blinding; use exact shortest-round-trip observations; retain the declared floor, 8192-output-token cap, 300 s attempt timeout, 5/20 s backoffs and entropy, with concrete context/total-token/spending ceilings required before paid work. C5 is a later implementation authorization gate: no E9 calls are permitted in this research turn; an actual permitted-implementation decision is required, not authority manufactured by a protocol paragraph. Design acceptance does not itself freeze or run anything.

Please report only specific blocking design contradictions in v0.3 if any. Otherwise save v0.3 **unchanged** as a Claude Science artifact, save a short `round3_disposition.md` stating your actual review conclusion and unresolved readiness gates, and repoint project scientific notes to v0.3 (NOT FROZEN). Explicitly distinguish agreement with a design choice from empirical validation. Retain the source brief's common IDs and verified evidence limits; no new broad literature search is necessary unless a concrete factual contradiction needs it. Do not claim every R is closed on your authority alone; Codex will reconcile independent reviews and synchronize final status.
