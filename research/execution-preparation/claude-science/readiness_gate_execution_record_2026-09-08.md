# Readiness gate execution record — 2026-09-08 (Claude Science)

**Authority basis:** Chris's 2026-09-08 instruction "Go ahead and perform all three steps in the Claude Science project", relayed by Codex, is treated as new proceed authority for gated execution. It supplied **no numeric USD ceiling, no credential, no measured readiness result**; those are recorded below as concrete requirements, not filled in. No human signature is invented. Protocol v0.4 (`0df9242f…3eda5`) is unchanged and **unfrozen**. **No paid generation or token-count request was made; no real mapping exists; no study objective was evaluated; no gate is claimed closed.**

Repository state read: `main` at `b89297cfbbca1a922110ef22c9c157e3f63f7fa7` (tarball fetched via GitHub API; all 27 `research/provider/verification.json` hashes match). Codex's PR #4 package at `448c4f4f…` (`README.md` SHA-256 `7d282d0d…`, `manifest.json` `6621bf9b…`) was read for reconciliation. No GitHub or Linear mutation was made from this side.

## 1. Performed in this session (actual, reproducible)

| # | Work | Evidence | Limits |
|---|---|---|---|
| P1 | Independent re-run of the repository test suite at `b89297cf` in a fresh conda env (CPython **3.11.16**, numpy 2.4.6, scipy 1.17.1, cma 4.4.4, pytest 9.1.1, pip-audit 2.10.1, ruff 0.16.6) | **233 passed, 1 warning, ~57 s**; `ruff check`/`format --check` clean on 20 files (`repo_pytest_rerun.log`). First run 231/233 until pip-audit was installed — environment gap, not a code defect | Python patch 3.11.16 ≠ uv.lock's 3.11.15; conda, not uv; therefore not the study environment |
| P2 | Direct retrieval of OpenAI primary documentation (13 pages) and openai-python v3.9.0 sources; SHA-256 of every retrieved file | `documentation_sources_2026-09-08.json`; contract in `sol_chat_request_contract_2026-09-08.md` | Mutable pages; retrieval date 2026-09-08 |
| P3 | Candidate Sol Chat adapter `qbridge_ext/sol_chat_adapter.py` implementing the runner's `transport(request, timeout)` contract with SDK `max_retries=0`, forbidden-field enforcement, usage/identity capture, protected raw-byte retention before parse, authority binding and explicit halt policy | **53 offline tests pass** (`ext_pytest.log`), two using the real `ProcessExecutor`; ruff clean | Mocked HTTP only; **never sent a request** |
| P4 | `qbridge_ext/study_coordinator.py`: §8.1 schedule (RS → CMA-ES → LLM) over one journal, global no-replay, `SpendLedger` reserve-then-reconcile with non-clipping, overrun halt, unknown-retains-full-cap | tests incl. schedule derived from protocol text | Scheduling/accounting component only — not the full launcher, recovery, masked export or freeze tool |
| P5 | Four fabricated E9 fixtures generated through the actual renderer; F2 combined text 103,146 B reproduces Codex's PROMPT_BYTE_BOUND figure; F4 = +96 B | `e9_fixtures_2026-09-08.tar.gz`, `fixtures_summary.json` | No token count exists for them (no provider call; no local tokenizer) |
| P6 | Budget scenarios with the unchanged `qbridge.budget.calculate_budget` (six scenarios, 12 E9 attempts) | `budget_scenarios_2026-09-08.json`; table in the proposal | Conditional ceilings; `spending_authorized: false` in every output |
| P7 | Synthetic private-storage demonstration: 0700 temp dir, `custody.create_private_mapping` produced a 0600 file, refused a path inside the public repo; directory deleted afterwards | `EXPOSURE_LOG_claude_science_2026-09-08.jsonl` (STORAGE-DEMO) | Synthetic; **no real mapping and no real protected location created** |
| P8 | Proposed analysis-lock record: hashes of `analysis/locked_analysis.py` (`a81a909c…`), `analysis.py` (`44225d19…`), `custody.py` (`9a7c5154…`), `journal.py` (`4381e014…`), protocol, `uv.lock`, `pyproject.toml`, plus invocation and input schema | `analysis_lock_record_proposed_2026-09-08.json` | Proposed manifest content, not a freeze |
| P9 | Session exposure log: 5 rows, `study_objective_evaluations: 0`, `experimental_model_calls: 0`, `token_count_calls: 0` for every row | `EXPOSURE_LOG_claude_science_2026-09-08.jsonl` | Covers this session only; Codex's workflows log separately |
| P10 | Independent code review findings incorporated with regression checks before any readiness statement: 4 blocking (schedule order, ledger clipping, authority binding, retention/enforcement), 6 focused budget/contract points, and 2 reproduced integration blockers — R5 (halt/fingerprint state now durable in the journal and re-read before every dispatch, fork-safe) and R6 (exact serialized request enforced in a pre-send hook; altered allowed-field values cause zero dispatches); authority validation completed (construction-time validation, rates/L/price source bound, zero rates fail closed) | proposal §5, §7 | — |
| P11 | **Not implemented (stated honestly):** whole-study launcher wiring the coordinator to `ArmRunner` for all 120 units; crash-recovery driver; masked-row export to the locked analysis; freeze-manifest writer; any live request. `study_coordinator.py` is a scheduling/accounting component only | — | These remain separate work before freeze |

## 2. Gate-by-gate status — explicit: **G-ENV OPEN · G-CMA OPEN · G-MODEL OPEN · G-TRANSPORT OPEN · G-CUSTODY OPEN · G-LOCK OPEN · G-EXPOSURE OPEN · G-STORAGE OPEN · G-RUNTIME OPEN · G-COST OPEN** (10/10 open)

| Gate | Performed here | Still required (measurement or decision) | Smallest concrete decision / action |
|---|---|---|---|
| G-ENV | Fresh-env reproduction of 233 tests; version capture; SDK 3.9.0 and httpx 0.28.1 identified as new study-runtime dependencies | Study environment must be built from `uv.lock` (3.11.15) with `openai`/`httpx` added and locked by Codex; exact-release documentation reconciliation | Codex: add `openai==3.9.0`, `httpx` to the lock and re-run the suite there |
| G-CMA | Nothing new (Codex's ask/tell characterization and option dump stand) | Bind into manifest | none |
| G-MODEL | Complete candidate **wire payload** (not verified identity/decoding); identity/usage capture code; explicit list of unresolved contracts | (a) sampling-parameter contract for `gpt-5.6-sol` — **undocumented**; (b) `system_fingerprint` population — unmeasured; (c) Chat input-token ceiling L_in — no counting contract; (d) 200-row worst-case wording vs. 190-row renderer limit; (e) acceptance of the full request — E9 | Chris/Codex: accept the Tier B inference as a recorded pre-freeze amendment basis **or** require a counting contract; decide whether an undocumented sampling default can satisfy §6.1 (it does not as written) |
| G-TRANSPORT | Adapter with error mapping, one-attempt-per-call proof, retention before parse, durable halt policy, pre-send enforcement; runner integration on stub objective and forked executor | Two distinguishable evidence classes: (a) **mocked control-flow checks** for 429/5xx/timeout/rejection paths — done offline, not live; (b) **actual accepted E9 response metadata** (acceptance, usage, identity fields, latency for four requests) — requires authorized E9. Inducing live failures is not required; no live measurement is invented | Authorized E9 for (b) |
| G-CUSTODY | Option (b) code exists in repo. **Codex evidence (2026-09-08, Codex's statement, not reinspected here, not a new Chris signature):** role selection recorded — operator = automated launcher under the `chrisdukes` account (not yet implemented/running); analyst = `analysis/locked_analysis.py` under that account (not run); custody process owner = the `chrisdukes` account via delegated automated setup. This is a location/role selection, not whole custody implementation or blindness | Register entry to be written by Codex (Chris delegated technical operation, so this need not be punted to him): operator = the automated launcher process under the execution account; analyst = `analysis/locked_analysis.py` executed automatically after sealing; custody process owner = the same automated process with Chris as accountable principal; Codex and Claude Science = implementers/reviewers, **not** independent custodians; **no independent evaluator blinding is claimed** | Codex records the register at the freeze commit |
| G-LOCK | Proposed lock record with hashes and invocation | Freeze-commit manifest entry; analysis environment pinned | Codex writes manifest at freeze |
| G-EXPOSURE | This session's log (zero exposure); Codex's repo log exists | Whole-workflow pre-freeze audit combining both logs | Codex merges logs |
| G-STORAGE | `ProtectedStore` + synthetic demo of the 0700/0600 checks. **Codex evidence (Codex's statement):** `/Users/chrisdukes/.local/share/quantum-bridge-lab/execution-2026-09-08` with `raw/`, `journals/`, `custody/`, `sealed-analysis/` exists, owner UID 502, mode 0700, outside Git, empty; **no mapping created** | `ProtectedStore` verification of that path at launch (not yet run); the directory's existence is not custody | none pending beyond launch-time verification |
| G-RUNTIME | Component/mocked only | Provider latency (E9 gives 4 points), full launcher, enforceable deadlines on the real machine | after E9 |
| G-COST | Six conditional scenarios (every L a planning hypothesis, none verified); ledger enforcement incl. per-dispatch price-validity check; explicit unknowns | Technical resolution of the input-ceiling basis first (Chat counting contract or a prospective pre-exposure amendment); then a numeric `AuthorityRecord` (`usd_ceiling`, `attempt_cap`, `authorized_by`, `recorded_at`, `purpose`, `scope`, `profile_sha256`, `price_valid_through_utc`, `rate_source`); account facts (region, tier, rejected-request billing) | Codex + Claude Science resolve the technical basis; only then is a concrete E9 figure put to Chris — no speculative cap is requested now |

## 3. Blockers (genuine; none can be routed around)

1. **Credential (observed setup gap):** no OpenAI credential is configured in this project (configured names: GitHub, JINA_API_KEY, OpenAlex; no values displayed). The adapter fails closed without `QBRIDGE_OPENAI_API_KEY`. Providing it to the execution machine (never to project artifacts) is Chris's action.
2. **Monetary authority:** no numeric USD ceiling exists, and none is requested until the input-ceiling basis is technically resolved (a Chat counting contract or a prospective, reviewed amendment). The adapter refuses construction without a validated `AuthorityRecord` bound to the ledger, profile and a price-validity instant.
3. **Storage and roles:** per Codex's statement, the protected directory now exists (empty, no mapping) and roles are selected; the register entry at the freeze commit and launch-time `ProtectedStore` verification remain. Not blockers requiring Chris.
4. **Identity standard:** if E9 returns a null `system_fingerprint` and sampling parameters remain undocumented, §6.1 is unmet as written; the honest paths are stop-before-paid-exposure or a reviewed pre-freeze amendment. Not decided here.

Because 1–2 are absent (and 3 is pending Codex's register entry), the recorded gate sequence stops at "permitted implementation and preflight (offline)". E9, gate closure, manifest freeze and both study stages were **not executed**, and no result is asserted. The proceed instruction is honored to the extent authority exists; the remaining steps resume once the credential, the authority record and the storage/custody register exist — no fresh "may I continue?" is needed for that.

## 4. Deliverables and hashes

Code (`qbridge_ext_2026-09-08.tar.gz`, portable ustar repack without AppleDouble members; final source SHA-256: adapter `f1534a106f51f4eb0d6d2f14e08a5072461f71a410f7108b132f09722b736740`, coordinator `4e557812fa32bae462a69130729d24c35267c7fb78074f4f5997ebb3dbd9cbf6`, fixtures generator `16c89cc2cb8f86b7443e8648c9ed80457427b02e91e86c83d1533df18d4c06d5`, tests `08f474d4805ae60a60d9ade772e703e63e75016a1ef70ffcfc5efa2c392d78c2`): `sol_chat_adapter.py`, `study_coordinator.py`, `e9_fixtures.py`, `tests/test_ext.py`, plus a hash-verified copy of protocol v0.4 used by the schedule test; the four `.py` files are also saved individually. Fetched documentation bytes: `openai_docs_sources_2026-09-08.tar.gz`. Per-file SHA-256 in `extension_hashes_2026-09-08.json`. Fixtures: `e9_fixtures_2026-09-08.tar.gz`. Records: `budget_scenarios_2026-09-08.json`, `documentation_sources_2026-09-08.json`, `analysis_lock_record_proposed_2026-09-08.json`, `EXPOSURE_LOG_claude_science_2026-09-08.jsonl`, `repo_pytest_rerun.log`, `ext_pytest.log`.

Codex is expected to check these artifacts independently and synchronize the repository and Linear; preserved originals (provider review v2 `169b61fd…`, retained disagreements) are untouched.
