# Round 2 challenge and artifact work order

Codex coordinating under Chris's delegation of expert research and pre-freeze corrections. Round 1 is now complete and preserved separately. This is a research/documentation request, not experimental execution or freeze. Do not run simulations, optimizer code, package installs or experimental model calls.

First read `research_evidence_brief_v1.md` and `protocol_governance_v1.md`, copied byte-for-byte into this workspace. They correspond to GitHub commit `2d50535a27ec0fd137f9f8ecd1144f369ab09a81`, repository clduab11/quantum-bridge-lab, branch research/protocol-hardening; Linear project quantum-bridge-lab and issue ADV-35. Context7/SciPy and Exa/arXiv evidence have been checked independently before this follow-up. Use their common source IDs and primary URLs. Preserve their content and save them as project artifacts. Treat evidence as review input; challenge it where warranted.

Round 1 materially improved the proposal but does not close R1–R10. Please address these concrete remaining defects, explain any disagreement, and save ONE complete corrected `ai_quantum_control_protocol_v0.2.md` plus a short `round2_disposition.md`. Keep v0.1 and Round 1 unchanged. Update the project's scientific notes to point to the new draft and source brief, with status NOT FROZEN.

1. Scope: remove the Hadamard development instance, held-out 6x6 grid, similarity-to-BB1 evaluation and all extra optimizer/reference runs. An exploratory label does not authorize extra objective variants. Use synthetic JSON/history fixtures and analytic identities for later engineering checks only. No performance-driven prompt tuning in this first experiment.

2. Preserve N=10, T=4pi, d=20, B=200, K=10 from the initial instance unless you identify a validity defect that truly requires changing them. BB1 being outside that encoding does not invalidate comparison; remove the unsupported BB1 solvability premise. Appending idle segments under detuning also changes a sequence's action, so representability alone does not demonstrate robustness. One conventional method may be CMA-ES with popsize=10 explicitly labeled a nondefault batch-matching choice, sigma0=0.5, no restarts; record all remaining settings at a pinned implementation revision. Do not claim this is the strongest comparator.

3. Correct metric name to mean process/entanglement infidelity on the fixed 25-point joint ensemble (PHY-NIELSEN-2002). Do not call it continuum/worst-case robustness or convert halved final infidelity into fewer evaluations or hardware shots. Remove invented floating-point resolution guarantees; a numerical floor, if used for log transforms, is an analysis design choice to be validated, not a measured error bound.

4. Statistics: Round 1's exact Wilcoxon plus percentile bootstrap does not generally test the median difference without symmetry; ties/zeros alter exact behavior. The asserted 80% power and calibration-derived 0.3-decade margin are unsupported. Consider this simpler coherent replacement: per seed block D=log10(max(I_AI,1e-12))-log10(max(I_CMA,1e-12)); primary estimand population median D, negative favors AI. For n=20 independent identically distributed blocks, distribution-free order-statistic interval [D_(6),D_(15)] has at least 1-2*sum(j=0..5) choose(20,j)/2^20 coverage (derive/check the value), conservatively allowing ties. No Wilcoxon or bootstrap required. A margin m=log10(2) is a transparent research choice, not a hardware-economic fact. Stage supports meaningful benefit only if interval upper<-m; excludes this prespecified median benefit if lower>-m; otherwise inconclusive. Report interval and full outcomes; these are not equivalence or universal no-benefit claims. Decide and justify one coherent procedure. Preserve the same n=20 plus independent n=20 replication, both always completed before unmasking, never conditioned on stage-one success. No pooled confirmatory analysis. Explain independence/stationarity assumptions, fixed pseudorandom seed convention, model drift limitations, resource-limited n and coarse attainable precision.

5. Fair action/data contract: Round 1 permits CMA raw R^24 but constrains LLM coordinates to [-1,1], and history rounds coordinates/objectives. Specify the identical admissible raw action domain for every method, overflow-safe disk mapping, ordering of coordinates, common numeric observation representation, and which raw/mapped vector enters CMA tell/history. If CMA uses only best initialization while AI uses all, state that as method choice; no equal-prior claim. Include full literal system/user prompt templates or complete normative text, strict JSON schema, stable ordering/tie policy and no hidden cross-run state.

6. Resource contract: 38 logical LLM calls contradicts up to5 infrastructure attempts per call. Define call vs attempt, fixed aggregate cap and time/token/output limits, ordering of transport retry and malformed-output correction, every failure path and attempt accounting. Prefer no run replacements or excluded failed blocks: after algorithm/API failure, keep incumbent from initialization through remaining forfeited slots and include the planned seed in endpoint analysis. Distinguish allotted slots, actual objective calls, valid evaluations and forfeits. Do not claim equal actual evaluations when invalid slots are forfeited. If simulator failure prevents a valid endpoint, mark study validity failure, preserve it and prohibit a confirmatory success claim. Never silently omit it. These are proposed corrections; justify another equally explicit nonselective approach if needed.

7. Blinding: a scorer knowing only A/B/C cannot select the AI-vs-CMA pair without mapping. Choose an executable procedure such as locked identical summaries for every ordered label pair, all outputs sealed, then custodian selection of the precommitted pair. Nuisance summaries are not additional hypothesis claims. No fictitious independent custodian: agents sharing context are not independent. Name custody/role setup as a readiness gate and describe a locked automated alternative and its limitations. No mapping or raw identity-bearing transcript in public Git.

8. Governance: record actual authority; no requirement for a forged human-authored artifact. Chris already delegated expert design decisions, but actual freeze/execution remains a separate recorded gate. Freeze the full configuration, prompts, analysis, seeds, failure/resource rules, environment and model fingerprint; capture model/default settings via metadata without inventing IDs or numeric defaults. Any unresolved operational value blocks freeze. Avoid a circular gate demanding scientific implementation before authorization to implement analytic checks: design review precedes permitted implementation/preflight, then run-manifest freeze precedes real evaluations.

9. Citation review: the app reviewer flags ungrounded Round 1 quotations. Use verified brief sources and precise bounded paraphrases, preserve dissent, remove unsupported references rather than fabricate retrieval provenance. The source-brief documentation headers differ from Context7 main; acknowledge these observations and require the actual installed version to be pinned later. Do not assert every trap-free theorem assumption fails; applicability has not been established for this constrained ensemble.

Please take the time needed for one rigorous pass. Save the corrected protocol and disposition as versioned project artifacts; list any remaining required corrections separately from execution-readiness gates. Include concise technical and layperson explanations and a speculative-commercial boundary with no target-hitting inference. No freeze, run, simulated result, or implemented blinding claim.
